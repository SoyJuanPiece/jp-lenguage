# Fibonacci en JP
fun fibonacci(n) {
    si (n <= 1) {
        retorna n
    }
    retorna fibonacci(n - 1) + fibonacci(n - 2)
}

para (i en rango(0, 15)) {
    muestra("fib({i}) = {fibonacci(i)}")
}
