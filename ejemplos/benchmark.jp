# Benchmark de JP — mide bucles while y recursión
variable i = 0
variable s = 0
mientras i < 1000000 {
    s = s + i
    i = i + 1
}
imprime("suma:", s)

funcion fib(n) {
    si n <= 1 { devuelve n }
    devuelve fib(n - 1) + fib(n - 2)
}
imprime("fib(22):", fib(22))
